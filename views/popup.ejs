<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Message</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js"></script>
    <style>
        .popup-overlay {
            opacity: 0;
            backdrop-filter: blur(8px);
        }
        .popup-content {
            transform: scale(0.5);
            opacity: 0;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-blue-600 via-purple-600 to-pink-500 min-h-screen">
    <div class="popup-overlay fixed inset-0 bg-black/50 flex items-center justify-center">
        <div class="popup-content bg-white rounded-xl p-8 max-w-md w-full mx-4 shadow-2xl">
            <div class="text-center">
                <div class="mb-4">
                    <svg class="w-16 h-16 text-blue-500 mx-auto" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                    </svg>
                </div>
                <h2 class="text-2xl font-bold text-gray-800 mb-4"><%= message %></h2>
                <p class="text-gray-600 mb-8">Redirecting in <span id="countdown">3</span> seconds...</p>
                <button 
                    onclick="window.location.href='<%= redirectUrl %>'"
                    class="w-full py-3 px-4 bg-gradient-to-r from-blue-600 to-purple-600 text-white rounded-lg font-semibold transition-all duration-300 transform hover:scale-[1.02] active:scale-[0.98]"
                >
                    Go to Login
                </button>
            </div>
        </div>
    </div>

    <script>
        // Animate popup appearance
        window.addEventListener('DOMContentLoaded', () => {
            // Fade in overlay
            gsap.to('.popup-overlay', {
                opacity: 1,
                duration: 0.3,
                ease: 'power2.out'
            });

            // Scale up and fade in content
            gsap.to('.popup-content', {
                scale: 1,
                opacity: 1,
                duration: 0.5,
                delay: 0.2,
                ease: 'back.out(1.7)'
            });

            // Countdown timer
            let countdown = 3;
            const countdownElement = document.getElementById('countdown');
            const countdownInterval = setInterval(() => {
                countdown--;
                countdownElement.textContent = countdown;
                if (countdown <= 0) {
                    clearInterval(countdownInterval);
                    window.location.href = '<%= redirectUrl %>';
                }
            }, 1000);
        });
    </script>
</body>
</html> 